{
  "buildCommand": "yarn install --frozen-lockfile && yarn workspace @snapshot-labs/sx build && yarn workspace ui build",
  "outputDirectory": "apps/ui/dist",
  "devCommand": "cd apps/ui && yarn dev",
  "installCommand": "yarn install --frozen-lockfile",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/ui/"
}
